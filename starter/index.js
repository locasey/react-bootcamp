import React from 'react';
import ReactDOM from 'react-dom';

/* console.log('hi'); */

const element = React.createElement('div', null, 'React element with JSX!');

ReactDOM.render(element, document.body);
